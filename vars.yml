---
repos:
  - ppa:neovim-ppa/stable

apt:
  - tmux
  - cmake
  - exuberant-ctags
  - silversearcher-ag
  - software-properties-common
  - python3-software-properties
  - python3-dev
  - python3-pip
  - python3-venv
  - neovim
  - sqlite3
  - zsh
  - htop
  - tree
  - speedtest-cli
  - golang
  - golang-go.tools
  - build-essential
  - libssl-dev
  - zlib1g-dev
  - libbz2-dev
  - libreadline-dev
  - libsqlite3-dev
  - wget
  - curl
  - llvm
  - libncurses5-dev
  - xz-utils tk-dev
  - libxml2-dev
  - libxmlsec1-dev
  - libffi-dev

pip:
  - ipython
  - flake8
  - autopep8
  - yamllint
  - pygments
  - glances

backup_dir: ~/backup

repo_dir: ~/dotfiles

prefix: "{{ repo_dir }}/files"

git:
  # powerlevel9K oh-my-zsh theme
  - { "repo": "https://github.com/bhilburn/powerlevel9k.git", "dest": "~/.oh-my-zsh/custom/themes/powerlevel9k" }
  # zsh-autosuggestions
  - { "repo": "git://github.com/zsh-users/zsh-autosuggestions", "dest": "~/.oh-my-zsh/custom/plugins/zsh-autosuggestions" }

dirs:
  # backup
  - { "dest": "~/backup" }
  # fonts
  - { "dest": "~/.local/share/fonts" }
  # neovim
  - { "dest": "~/.config/nvim/autoload" }

configs:
  # neovim
  - { "name": "init.vim", "src": "{{ prefix }}/init.vim", "dest": "~/.config/nvim/init.vim"}
  # zshrc
  - { "name": ".zshrc", "src": "{{ prefix }}/.zshrc", "dest": "~/.zshrc" }
  # tmux
  - { "name": ".tmux.conf", "src": "{{ prefix }}/.tmux.conf", "dest": "~/.tmux.conf" }
